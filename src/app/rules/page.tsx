import { RulesBrowserClient } from "@/components/sites/krds/app/rules-browser-client";

export default function RulesPage() {
  return (
    <main className="flex-1 bg-background">
      <RulesBrowserClient />
    </main>
  );
}
